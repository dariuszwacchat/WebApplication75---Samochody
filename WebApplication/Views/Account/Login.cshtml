@model LoginViewModel


<div class="center-container db">
    @{
        string password = "SDG%$@5423sdgagSDert";
        <h3>@password</h3>
    } 

    @Model.Result

    <form method="post" asp-action="Login" asp-controller="Account" class="login-form">
        <div class="form-group">
            <div class="df">
                <label asp-for="Email" class="form-label"></label>
                <span asp-validation-for="Email" class="c-red"></span>
            </div>
            <input asp-for="Email" class="form-input" />
        </div>
        <div class="form-group">
            <div class="df">
                <label asp-for="Password" class="form-label"></label>
                <span asp-validation-for="Email" class="c-red"></span>
            </div>
            <input asp-for="Password" value="@password" class="form-input" />
        </div>


        <button type="submit" class="form-button">Login</button>

        <p class="text-danger">@Model.Result</p>
    </form>
</div>